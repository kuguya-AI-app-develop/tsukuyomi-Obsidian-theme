# 验收记录 · 0.4.0

日期：2026-09-16。按用户选择，将月读主城与八千代舞台结合，保留 v0.3 的阅读排版。本版新增矢量场景、独立导航招牌和工作区饰线，已构建并安装到项目实验库。

## 自动检查与审查

- `npm test`：11 项测试通过。8 层 CSS 确定性构建、两幅 SVG 内嵌、根目录与 dist 一致；产物 34,971 字节（34.2 KiB），低于 50 KiB 预算。
- 两幅原创 SVG 通过现有静态资源校验，无脚本、外链、图片引用和动画。正文装饰回归检查通过，未增加正文图片、文字阴影或生成装饰。
- 5 项 Style Settings 配置通过；102 项静态属性值检查通过，133 项动态值按已实现的关键变量与颜色规则检查，不等同于浏览器完整计算验证。
- 默认色板已检查文字组合均达到 4.5:1，焦点达到 3:1。深色普通文字在所检查背景中的最低值为 10.36，浅色为 10.93；所有结果见 [checks-v0.4.txt](checks-v0.4.txt)。不涵盖任意用户配色和全部第三方插件状态。
- `npm run lab` 与 `git diff --check` 通过。只读审查未发现范围内的 P1/P2 问题，重点复核了 SVG 构建、窄窗格规则、打印隐藏以及原生空白页操作布局。

## 真实应用观察

环境为 macOS 上的 Obsidian 1.13.7，仅使用本项目实验库，无新增插件或应用升级。

| 场景 | 结果与证据 |
| --- | --- |
| 深色长文 | [截图 32](screenshots/32-identity-dark-reading.jpg)：独立街景招牌、朱红选中标记可见，笔记正文和标题仍为无场景装饰的纯色阅读面 |
| 深色空白页 | [截图 33](screenshots/33-identity-empty.jpg)：上方鸟居与光镜、下方楼阁和光鱼正常显示，中央新建、打开和关闭操作未被覆盖 |
| 浅色空白页 | [截图 36](screenshots/36-identity-light-empty.jpg)：通过原生命令面板切换浅色后观察，场景与独立深色招牌正常呈现 |

后续操作期间检测到用户正在操作实验库窗口，因此停止争用界面。没有将未完成的浅色长文截图、窄窗口或分栏交互记录为通过。实验库最后保留原生界面当前选择的浅色模式，此本地外观选择不纳入主题提交。用户此前已有的 `03-提示块与属性.md` 修改未改写、未纳入提交。

## 未测与手动复核

- 在 400px 窗口打开长文并收起侧栏，检查换行；在宽桌面内将空白页缩至 600px 以下，确认上下场景隐藏。
- 将文件树宽度缩至 240px 以下，检查招牌只保留居中文字。上述响应规则已经实现和审查，本版未完成 GUI 验证。
- 本版未复验实时预览、源码模式、Style Settings 插件交互、中文 IME、字号/文字间距放大、打印/PDF、移动端和长时性能；历史版结果不自动计入本版。
- 若已用 Style Settings 保存旧的 `0.10` 装饰透明度，该用户值会继续生效；需要本版效果可调整至 `0.70` 或重置此项。无插件时默认值直接生效。

安装目录为 `dist/Tsukuyomi/`。回退可在外观中选择默认主题，或从提交 `a3a2eeb` 取回 v0.3.0 的 `theme.css` 与 `manifest.json`。无需改动笔记。

---

# 验收记录 · 0.3.0

日期：2026-09-16。月读夜街与阅读改版已构建，并安装到项目独立实验库。以下为本版实际完成范围，历史验收不自动计入本版。

## 自动检查

- `npm test`：11 项测试通过，覆盖静态资源、动效限制以及正文装饰的正反例。新增正文检查阻止已知阅读选择器上的图片背景、文字阴影和生成装饰，并限制背景简写中的变量；这是针对本项目的回归检查，不是完整 CSS 级联或可访问性证明。
- 8 层 CSS 确定性构建，根目录与 dist 一致；安装 CSS 24,939 字节（24.4 KiB），低于 50 KiB 预算。当前没有内嵌图片或运行时网络资源。
- 5 项 Style Settings、79 项静态属性值通过；127 项动态属性值另按可验证范围处理，不声称全部经过浏览器计算验证。
- 默认深/浅正文对比度 10.68 / 10.93，次要文字 6.65 / 4.93；已检查文字组合均不低于 4.5:1，焦点不低于 3:1。任意用户配色、全部叠层与第三方插件不在此保证范围。
- `npm run lab` 与 `git diff --check` 通过。完整输出见 [checks-v0.3.txt](checks-v0.3.txt)。

## 真实 Obsidian 检查

使用 Computer Use 操作 macOS 上的 Obsidian 1.13.7，仅在 `Tsukuyomi Lab` 验证，未安装插件或升级应用。

| 场景 | 结果与证据 |
| --- | --- |
| 深色长文 | [截图 27](screenshots/27-nightstreet-dark-reading.jpg)：正文纯色、标题无光晕和重框、文件列表无底纹，40rem 默认行宽生效 |
| 多页滚动 | 实际向下滚动三页，查看第 22–28 段，再返回顶部；未见装饰覆盖正文。未采集长时性能基准 |
| 浅色长文 | [截图 28](screenshots/28-nightstreet-light-reading.jpg)：通过原生命令面板“切换浅色/深色模式”完成切换，随后恢复深色；不是开发工具样式类模拟 |
| 实时预览 | [截图 29](screenshots/29-nightstreet-editing.jpg)：原生编辑视图的中文、混排段落与标题正常显示；未对笔记执行编辑操作 |
| 空白标签页 | [截图 30](screenshots/30-nightstreet-empty.jpg)：灯笼与远檐位于边缘，原生新建、打开、关闭操作保留原位置 |

空白页宽度容器规则经过代码审查；窄窗格、窄窗口和低矮窗口的装饰隐藏已实现。准备进行本轮窄视口实测时 Mac 锁定，此项未完成，不能用 v0.2.0 窄屏截图代替。

本轮开始前已存在的 `03-提示块与属性.md` 本地修改保持原样，未纳入本轮提交。未改主力笔记库。

## 未测与后续手动检查

- 在实验库将空白页放入窄分栏，确认宽度不超过 600px 时灯笼与远檐消失；再用 400px 窗口打开长文，收起侧栏检查换行与横向溢出。
- Style Settings 面板与五项交互、用户字号/文字间距放大、系统减少动态效果、打印/PDF、中文 IME、移动端和全部插件场景未在本版复验。
- 本版完成深色实时预览观察；浅色编辑、源码模式、全部代码/表格/Canvas/Bases 操作尚未复验。

安装目录为 `dist/Tsukuyomi/`。回退只需在 Obsidian 外观中选择默认主题；如需旧版可从 Git 提交 `bf44d41` 取得 v0.2.0 的两个主题文件，无需改动笔记。

---

# 验收记录 · 0.2.0

日期：2026-09-16。无人物的舞台视觉改版已构建并安装到独立实验库。自动检查通过，完成下述真实 Obsidian 1.13.7 桌面渲染检查。下方保留 v0.1.0 历史记录，未重复执行的旧项目不视作新版已通过。

## 本版自动检查

- `npm test` 通过：6 组 `node:test` 回归，包含静态 SVG 正例及外链、脚本、事件属性、实体、错误 XML、关键帧、动画属性等负例。
- 8 层 CSS 与一份云纹 SVG 确定性构建、根目录和 dist 一致；安装 CSS 为 32,561 字节（31.8 KiB），低于 50 KiB 预算。没有人物资源或关键帧动画。
- 5 项 Style Settings 配置、152 项静态属性值通过，166 项动态值不作通用属性词法判定；默认颜色另行解析。
- 深/浅正文对比度分别为 12.64 / 10.93，次要文字为 7.16 / 4.93；所检查的正文、链接、按钮、标签、提示块状态均达到 4.5:1，焦点达到 3:1。检查不覆盖所有叠层、任意用户配色或插件。
- `npm run lab` 与 `git diff --check` 通过。完整自动检查输出见 [checks-v0.2.txt](checks-v0.2.txt)。

## 本版真实应用检查

通过 Computer Use 操作本项目实验库，未改主力库、未安装插件或升级 Obsidian。开发工具用于可逆的样式类预览与视口模拟，没有执行注入脚本；结束后恢复深色、展开侧栏、关闭模拟与开发工具。

| 场景 | 结果与证据 |
| --- | --- |
| 默认深色首页 | [舞台首页](screenshots/19-stage-dark-home.jpg)：边框、侧栏云纹与 CSS 月轮正常显示，无人物 |
| 浅色首页 | [月白](screenshots/20-stage-light-home.jpg)：在真实 Obsidian 中切换原生 `theme-light` 样式类查看；本版未完成外观下拉菜单切换流程的复验 |
| 空白页 | [空白舞台](screenshots/21-stage-empty.jpg)：月轮与原生新建、打开、关闭操作不重叠 |
| 窄视口 | [400px 桌面视口模拟](screenshots/26-stage-narrow.jpg)：收起原生侧栏后，首页文字、月轮与提示块正常排布；这不是移动端实机验收 |

代码审查确认首页标题采用普通文本布局，保留粗体、链接等行内元素的正常换行；简洁模式与打印选择器覆盖本版新增月轮。所有人物、人物设置及相关动画已从分发内容移除。

## 本版未测与回退

Style Settings 插件面板的实际交互、系统减少动态效果切换、打印/PDF、移动设备、中文 IME、长时性能及全部编辑/插件场景尚未复验。过渡效果限制与打印规则目前包含静态检查和代码审查，不能代替上述实测。

安装产物在 `dist/Tsukuyomi/`。回退可在 Obsidian 外观中切换为默认主题，或从 Git 的 v0.1.0 初始提交恢复原主题文件；无需改变笔记。

---

# v0.1.0 历史验收记录

日期：2026-09-16。结论：已交付可构建、可安装的首版，自动检查通过，完成 macOS 桌面冒烟检查。仍有下表所列未测场景，不视为全面兼容认证。

## 环境与边界

- Node.js 24.15.0，npm 11.12.1。
- 实际运行 Obsidian **1.13.7**；安装程序版本 1.7.7 是外壳版本，不是本次应用版本。
- 仅在本项目 `lab/Tsukuyomi Lab/` 操作；实验库未安装社区插件，Style Settings 不在场。
- 初版验收时未修改主力库的文件或配置，未安装/升级 Obsidian，也未提交 Git 或发布主题。后续仓库创建与推送遵循用户另行授权，详见 `PROJECT.md`。

## 自动检查

实际执行了 `npm test`、`npm run lab`、`node --check scripts/check.mjs`。最终检查输出见 [checks.txt](checks.txt)。构建产物约 23.1 KiB，无运行时依赖。

覆盖 manifest、八层 CSS 的确定性构建与分发一致性、CSS 解析、五项设置的 YAML 配置、主题变量引用、1.13 完整颜色格式、资源与动效限制，以及默认色板的对比度。

CSS 解析器确认 74 项可静态判定的属性值；121 项动态属性值跳过通用词法匹配，关键颜色变量另行解析。不能把此检查等同于浏览器完整计算样式验证。

默认色板的实测计算值（取所检查背景/状态的最小值）：

| 项目 | 深色 | 浅色 | 阈值 |
| --- | ---: | ---: | ---: |
| 正文 | 13.42 | 10.93 | 4.5 |
| 次要文字 | 7.60 | 4.93 | 4.5 |
| 内部链接 | 9.84 | 5.42 | 4.5 |
| 强调按钮 | 9.84 | 5.91 | 4.5 |
| 选中文字 | 8.62 | 7.83 | 4.5 |
| 标签及悬停 | 5.44 | 4.75 | 4.5 |
| 提示块标题 | 6.21 | 4.64 | 4.5 |
| 提示块中的链接 | 5.92 | 4.84 | 4.5 |
| 焦点边框 | 8.74 | 5.09 | 3.0 |

以上不涵盖用户任意自选强调色、第三方插件、全部嵌套/透明状态。对比度算法使用标准相对亮度和 alpha 合成，色彩混合按主题使用的 sRGB 处理。

另在项目内隔离夹具中验证了根 CSS 文件和 dist 目录为符号链接时构建会拒绝写入，目标哨兵文件保持不变。夹具位于 `tmp/build-safety-ytsx80h6/`，不随主题分发。

## 真实桌面检查

通过 Computer Use 操作 Obsidian，以下均来自真实应用界面，不是网页模拟。

| 场景 | 实际结果与证据 |
| --- | --- |
| 安装、主题选择、无插件默认值 | 已加载 Tsukuyomi；[设置截图](screenshots/01-theme-settings.jpg) |
| 深浅阅读视图 | [最终深色](screenshots/17-final-dark.jpg)、[最终浅色](screenshots/16-final-light.jpg) |
| 深浅实时预览 | [深色](screenshots/04-dark-live-preview.jpg)、[浅色](screenshots/07-light-live-preview.jpg)，检查中文、属性和提示块的可见布局 |
| 深浅源码模式 | [深色](screenshots/05-dark-source.jpg)、[浅色](screenshots/06-light-source.jpg)，Markdown 和前置属性可见 |
| 中文混排粘贴与撤销 | 编辑器中粘贴中文、English 和标点后撤销，确认验收标记未留在笔记中；不等同于中文 IME 组合输入验收 |
| 代码块 | [浅色截图](screenshots/09-light-code.jpg)，语法色与边界可见；没有据此声称所有长行横向滚动均通过 |
| 命令面板、快速切换、搜索 | [面板](screenshots/10-light-command-palette.jpg)、[切换器](screenshots/11-light-switcher.jpg)、[搜索](screenshots/13-light-search.jpg)；实验库搜索“月光”返回 52 项结果 |
| 中文长文滚动 | [浅色长文](screenshots/12-light-long-text.jpg)，完成多页滚动；未采集性能基准 |
| Canvas、Bases | [Canvas](screenshots/14-light-canvas.jpg)、[Bases](screenshots/15-light-bases.jpg)，样本可打开，卡片与 9 行索引正常显示；不包含拖拽等全部操作 |
| 默认亮海青按钮 | [深色主要按钮](screenshots/18-dark-primary-button.jpg)的文字为深色；只打开导出设置弹窗，未执行 PDF 导出 |

1.13 原生强调色的代码核对结论见 [native-color-reference.txt](native-color-reference.txt)。按钮遵守原生文字变量，且保留默认亮色按钮的深色文字。自定义主题色的所有组合未进行 GUI 穷举。

## 未完成的验收

- Style Settings 实际安装、五项交互、沉浸月轮显示及卸载回退；目前完成的是配置/默认值与样式静态检查。
- 中文 IME 候选窗、组合输入、连续输入；以及全部键盘焦点路径。
- 移动设备、软键盘、触控、窄屏实机和系统减少动态效果切换。
- 分屏、弹出窗口、关系图、数学/Mermaid、嵌入、任务和属性编辑的完整交互回归。
- 宽表格/超长代码的全部滚动状态、打印/PDF 实际输出。
- 与默认主题的长时间性能对照、多插件组合、其他系统和 Obsidian 版本。

## 阶段状态与下一步

P0–P3 已完成首版实现，P4 的浅色及移动/打印 CSS 已实现但移动、打印实测待做；P5 已完成自动检查与桌面冒烟，完整回归待做。优先在实验库验证真实中文输入法与 Style Settings，再决定是否安装到日常笔记库。回退只需在外观设置中选择默认主题。
