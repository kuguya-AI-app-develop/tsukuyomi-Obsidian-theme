---
tags: [验收, 技术]
---
# 表格与代码

## 列的边界

| 状态 | 章节 | 中文段落 | English | 数量 | 日期 | 备注 |
| :--- | :--- | :--- | :--- | ---: | :--- | :--- |
| 进行中 | 月下记录 | 一行普通的中文说明 | Moonlit notes | 128 | 2026-09-16 | 观察表头与悬停 |
| 已完成 | 创作舞台 | 把文字整理成自己的故事 | A quiet writing space | 64 | 2026-09-17 | 当前行与边框需清楚 |

| 很宽的列 A | 很宽的列 B | 很宽的列 C | 很宽的列 D |
| --- | --- | --- | --- |
| abcdefghijklmnopqrstuvwxyz0123456789 | abcdefghijklmnopqrstuvwxyz0123456789 | abcdefghijklmnopqrstuvwxyz0123456789 | abcdefghijklmnopqrstuvwxyz0123456789 |

## 语法与长行

行内代码 `const moon = "月读"` 不应挤压行高。

```javascript
// 中文注释与 English 混排
const moon = { phase: "满月", light: 0.72, visible: true };
function writeTonight(note) {
  if (!note) throw new Error("请留下一句话");
  return `${moon.phase} · ${note}`;
}
console.log(writeTonight("一段用于横向滚动验收的长行。".repeat(20)));
const longLine = "abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
```

```css
.theme-dark {
  --background-primary: #111821;
  --text-normal: #e6edf3;
}
```

```python
def remember(words: str) -> str:
    return f"月下记录：{words}"
```

[[00-欢迎来到月读|返回月读]]
