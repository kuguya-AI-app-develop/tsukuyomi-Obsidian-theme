# Tsukuyomi · 实施规范

目标：交付受《超时空辉夜姬！》视觉启发的非官方 Obsidian CSS 主题 v0.1.0，可构建、可安装，中文正文优先，装饰可关闭，不依赖插件。

## 依据与范围

依据 2026-09-16 对话「评估桌面助手优化」的完整正文。对话中提到的 `TSUKUYOMI_PROJECT_GUIDE.md` 附件未提供；本文件是根据可读正文整理的实施规范，不冒充原附件。

- 深色「月读夜景」与浅色「月白」，沿用 Obsidian 外观切换。
- 深蓝灰、海青、朱红、月金，保留系统及用户字体设置；行高 1.75、正文宽度 44rem。
- 6 / 10 / 14px 圆角；文件选中边标记、当前标签细线、可见键盘焦点。
- 阅读视图、实时预览、源码模式；文件树、属性、提示块、代码、表格、搜索、命令面板、Canvas 和关系图基础配色。
- 两类原创 Callout：`tsukuyomi` 与 `stage`；保留原生提示块语义。
- 五项可选 Style Settings：沉浸装饰、正文宽度、界面密度、装饰强度、主题动效（默认关闭）。主题默认减少动效，且尊重系统设置。
- 仅原创 CSS 几何装饰，不捆绑官方图片、Logo、音乐、字体；无远程资源、持续动画或全窗模糊。

## 实施边界

安装产物为 `dist/Tsukuyomi/manifest.json` 和 `theme.css`。独立实验库为 `lab/Tsukuyomi Lab/`。不改主力笔记库、不安装升级 Obsidian。用户于 2026-09-16 授权使用本机 GitHub 账号 `ArisaTaki`，创建并推送至 `kuguya-AI-app-develop/tsukuyomi-Obsidian-theme` 组织仓库；作者署名为 `ArisaTaki`。首次仓库创建采用私有可见性，公开仓库、创建 Release 或提交社区目录另行按用户指示执行。

不制作网页替代品，不增加主题插件、助手或业务功能，不全局改写 CodeMirror 行布局，不覆盖用户字号设置。

## 结构与阶段

`src/` 按设置、色板、语义、工作区、编辑器、组件、装饰、移动及打印分层；Node 构建按固定顺序输出根目录 `theme.css` 及安装目录。开发检查依赖仅用于解析 CSS 与设置 YAML。

1. P0：核对官方规范、锁定视觉和版本。
2. P1：深色基础和排版。
3. P2：核心组件和原生功能兼容。
4. P3：原创装饰和五项设置。
5. P4：浅色、窄屏及打印规则。
6. P5：自动检查、真实桌面验收、安装及回退文档。

## 风险与验证

主要风险为 Obsidian 版本选择器变化、Callout 颜色格式、浅色强调色对比度、编辑器交互和用户设置优先级。优先原生变量，保持选择器简单。

自动检查覆盖：manifest、CSS 语法及可验证的属性值、设置 YAML、构建可重复性、分发一致性、资源与动效预算、两种模式基础/选中/按钮颜色对比度。

真实桌面检查仅在实验库进行，记录应用版本与截图。中文 IME、移动端软键盘、长时性能及未执行的流程必须标明未测，不能用静态检查代替。无需插件时也应正常显示。所有最终验证状态记录在 `docs/VALIDATION.md`。
