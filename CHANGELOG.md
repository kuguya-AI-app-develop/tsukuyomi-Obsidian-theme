# 更新日志

## 1.0.5 — 2026-09-17

- 为 Mermaid 大图增加横向滚动容器，保留渲染字号；节点位置、分组和连线路径仍由 Mermaid 决定。
- 为流程图、时序图和状态图适配主题深浅配色，类图、ER 图等其他类型保留原生配色，不新增 JavaScript 或插件。
- 增加单篇笔记的 `tk-mermaid-original` CSS 类，可保留原生图表配色并继续使用滚动容器，适用于自定义 `theme` 或 `themeVariables` 的图表。

## 1.0.4 — 2026-09-16

- 针对 iPhone 14 Pro 反馈调整窄屏空白页：保留紧凑舞台与操作菜单，为原生顶部和底部控件预留空间。
- 恢复紧凑侧栏铭牌，移除空文件工具栏残留的扁框。
- 将动效启用的窗口与窗格最小尺寸降至 `320 × 480`，保留活动窗格、减少动态效果、静态模式及简洁模式保护。
- `npm test` 24/24、Stylelint 0 errors / 0 warnings；CSS 为 81,856 字节，已安装到实验库。原有两处打印分页局部兼容例外仍保留。
- 四种竖屏尺寸的桌面浏览器模拟检查通过，包含场景、SVG 动画选择、菜单点击命中和无横向溢出；横屏、窄窗格回退及桌面布局对照通过。这些检查使用本机 Obsidian CSS 与模拟 DOM，仍需 iOS 真机复核；不据此宣称 1.0.4 官方审核通过或客户端目录已同步。

## 1.0.3 — 2026-09-16

- 正式复核 Completed，无 Error，官方 CSS 提示由 16 条降为 2 条；保留有浏览器支持依据和 PDF 验证的两条打印分页规则。

- 用限定范围的打印选择器和原生变量替换 11 处打印 `!important`。
- 将 3 处链接装饰属性改为 Obsidian 原生链接变量及编辑器虚线边框，保留未解析链接的识别方式。
- 保留打印分页的 `break-after: avoid` 与 `break-inside: avoid`，各使用一条仅作用于下一行的兼容规则忽略注释，纠正 doiuse 将打印分页归入多栏特性的诊断；不将全部 16 条提示描述为实际渲染缺陷。
- 增加贡献指南入口，并记录上述兼容处理的依据；主题场景与动画不变。

## 1.0.2 — 2026-09-16

- 根据官方首轮审核的语言提示，主 README 改为完整英文，并保留独立中文版。
- 更新公开发布与审核记录；视觉、动画、素材及许可范围不变。
- 完成 Obsidian Community 官方审核并发布 1.0.2 官网条目，保留非阻断的 CSS 提示；客户端目录同步另行验证。

## 1.0.1 — 2026-09-16

公开提交准备与许可打包更新，视觉、布局和动效沿用 1.0.0。

- 为维护者有权许可的原创软件代码采用 MIT License，并在 NOTICE 中明确第三方角色设计等权益的排除范围。
- 在分发的 `theme.css` 头部随附完整许可证与范围说明，保留两文件安装方式。
- 更新公开发行包链接、安装文档与社区目录审核状态；审核通过前仍使用手动安装。
- 不改变视觉 CSS 声明或素材，平台实测范围与未测限制保持不变。

## 1.0.0 — 2026-09-16

首次正式版本，采用已确认的月读城市与八千代舞台视觉。

- 提供深色「月读夜景」和浅色「月白」，支持阅读、实时预览与源码视图。
- 正文保持纯色背景，默认 `40rem` 宽度、`1.75` 行高，保留用户字体与字号。
- 加入侧栏灯牌、奶油色选中项、青绿与珊瑚色框线，以及边缘青色斜纹。
- 空白页采用独立的舞台、菜单和城市布局；四位矢量伙伴分散在上方，骨架鱼在下方两侧单向游过并淡出接续。
- 提供五项可选 Style Settings 配置；减少动态效果和静态模式使用静态图，窄小窗格、简洁模式与打印隐藏场景。
- 使用内嵌 SVG 声明式动画，安装产物仅含 CSS 与清单，无运行时 JavaScript、网络请求或插件依赖。
- 提供确定性构建、素材生成检查、独立实验库及手动安装说明。

最低要求为 Obsidian 1.13.7，真实应用检查限于 macOS 上的 1.13.7。其他平台、中文输入法组合输入、Style Settings 面板、打印/PDF、长期性能及完整插件兼容性尚未全部验证，详见 [验收记录](docs/VALIDATION.md)。

尚未收录到 Obsidian 社区主题目录；安装方式及当前访问、授权状态见 [README](README.md)。
