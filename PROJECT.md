# Tsukuyomi · 实施规范

目标：交付受《超时空辉夜姬！》中月读空间启发的非官方 Obsidian CSS 主题 v1.0.2，可构建、可安装，结合月读城市与八千代舞台的空间特征，适合中文长文阅读与编辑，不依赖插件。

## 依据与范围

依据 2026-09-16 对话「评估桌面助手优化」的完整正文。对话中提到的 `TSUKUYOMI_PROJECT_GUIDE.md` 附件未提供；本文件是根据可读正文整理的实施规范，不冒充原附件。

用户要求结合月读城市与八千代舞台，保持原作辨识度并避免干扰阅读；最新范围包含 DOGE、FUSHI、粉色伙伴，以及彩叶狐狸装扮头部的四位伙伴矢量二创。狐狸头依据官网正面装扮立绘重绘，用户截图用于补充角度参考，仅绘制头部；粉色伙伴依据用户图片重绘，官方名称尚未确认。v0.7.1 继续使用原创几何 SVG 表达叠檐城市、骨架鱼、鸟居与镜面舞台，并参考用户提供的 irop.one 强化海军蓝与奶油色灯牌、青绿与珊瑚偏移边框。用户授权独立图层动效；游鱼采用连续路径变形和单向淡出接续，角色使用局部轻微动效，建筑保持静止；纯色阅读区域不加入纹样、标题发光或首页海报卡片。

- 深色「月读夜景」与浅色「月白」，沿用 Obsidian 外观切换。
- 墨蓝、海青、朱红与月金，保留系统及用户字体、字号；行高 `1.75`、正文默认宽度 `40rem`。
- 基础组件使用 `6 / 10 / 14px` 圆角；侧栏灯牌和工具区强化青绿、珊瑚框线，选中文件使用不透明奶油色底和深色文字；标题栏与工具区边缘保留青色短斜线，左侧导航标签区与窄边条使用淡几何纹理；保留清晰的选中标记和键盘焦点。
- 阅读视图、实时预览、源码模式使用不透明纯色背景；文件列表不叠加场景图案，笔记标题不承担场景装饰。
- 覆盖文件树、属性、提示块、代码、表格、搜索、命令面板、Canvas、关系图和 Bases 的基础配色。
- 两类原创 Callout：`tsukuyomi` 与 `stage`；保留原生提示块语义。
- 五项可选 Style Settings：简洁模式（默认关闭）、正文宽度（`40rem`）、界面密度（标准）、空白页场景透明度（`0.70`，范围 `0–1`、步长 `0.05`）、静态场景（`tk-disable-motion`，默认关闭）。无插件时默认值也生效。
- 文件列表上方单独保留静态灯牌；空间足够时，空白页的鸟居与光镜居中，原生操作使用其后的独立布局行，水波跟在操作区后，城市固定在底部。四位伙伴使用独立的 `1200 × 800` SVG 图层，DOGE 位于上方左侧、FUSHI 位于上方右侧，粉色伙伴位于上方中央，狐狸头位于中央偏左的留白；鱼保留在下方两侧。装饰不接收指针事件。简洁模式关闭招牌、装饰框线与场景。旧 `tk-home` 类不触发特殊布局。
- 空白页窗格或窗口的宽度不超过 `600px` 或高度不超过 `560px` 时，隐藏城市、鱼、伙伴、鸟居、光镜和水波六个图层，并恢复原生居中的操作区；打印同样隐藏六层场景，窄窗口同时隐藏侧栏招牌。
- 空白页动效默认启用，不依赖插件：两条鱼分别使用 `1.2s` / `1.4s` 的鱼身、尾和鳍变形周期，沿 `18s` / `22s` 时间轴交替呈现单向游过、淡出和反向鱼淡入；可见期间朝向固定；DOGE 尾部 `4s`、FUSHI 浮动 `5s` 与鳃部 `6s`、粉色伙伴浮动 `6s`、狐狸头轻摆 `7s`；光镜 `5s`、水波 `4s`。城市建筑、鸟居和笔记内容保持静止。
- 动态鱼与角色 SVG 仅在活动空白页、窗口和窗格均至少 `601px × 561px`、系统允许动态效果且未启用简洁或静态模式时被 CSS 选用。其他情况下使用静态鱼和角色，或隐藏场景；光镜与水波保留对应的 CSS 动效限制。主题不因窗口失焦主动暂停动效。
- `tk-disable-motion` 同时关闭 `140ms` 的颜色、边框和阴影过渡；替代旧 `tk-enable-motion` 设置。不使用全窗模糊。
- DOGE、FUSHI 与彩叶狐狸装扮头部根据官方角色图制作矢量二创；狐狸另参考用户提供的角度，粉色伙伴按用户图片重绘。不为装扮或未确认身份的形象另取官方吉祥物名称。粉色伙伴的身份尚未确认。角色设计并非本项目原创。参考图片不进入安装产物，不捆绑官方 PNG、Logo、音乐或字体，无远程资源。SVG/CSS 格式本身不免除既有素材的授权要求。

## 实施边界

安装产物为 `dist/Tsukuyomi/manifest.json` 和 `theme.css`。独立实验库为 `lab/Tsukuyomi Lab/`。不改主力笔记库、不安装升级 Obsidian。用户于 2026-09-16 授权使用本机 GitHub 账号 `ArisaTaki`，创建并推送至 `kuguya-AI-app-develop/tsukuyomi-Obsidian-theme` 组织仓库；作者署名为 `ArisaTaki`。首次仓库创建采用私有可见性，本轮用户进一步要求将已确认版本推送为首个正式版并准备上架，因此创建 1.0.0 GitHub Release；用户随后明确授权完成公开仓库、采用适当许可、提交官方社区目录并跟进至审核通过；据此继续处理发布与审核。

不制作网页替代品，不增加主题插件、助手或业务功能，不全局改写 CodeMirror 行布局，不覆盖用户字号设置。运行时只有 CSS 与内嵌 SVG 声明式动画，无 JavaScript 或网络请求。

## 结构与阶段

`src/` 按设置、色板、语义、工作区、编辑器、组件、装饰、移动及打印分层。Node 构建按固定顺序合并样式，并将 `assets/tsukuyomi-{city,gate,fish,fish-swimming,mirror,mascots,mascots-living}.svg` 与 `assets/stage-clouds.svg` 内嵌到 CSS，输出根目录 `theme.css` 及安装目录；运行时不请求网络。场景资产拆成静态建筑与独立动效层；云纹沿用本项目已有原创文件。开发检查依赖用于解析 CSS、设置 YAML 与官方 Stylelint 检查。`node scripts/generate-fish.mjs` 生成游鱼，`node scripts/generate-mascots.mjs` 生成角色动静两版，并调用 `scripts/render-mendako.mjs` 与 `scripts/render-fox.mjs` 的纯函数绘制粉色伙伴和狐狸头；这两个模块没有独立 CLI。构建通过纯渲染函数核对生成结果并拒绝旧资产，生成器不进入安装产物。CSS 预算为 `80KiB`，每个解码后的 SVG 预算为 `10KiB`。

1. P0：核对原作制作资料、阅读参考与官方规范，锁定视觉和版本。
2. P1：深色基础与阅读排版。
3. P2：核心组件和原生功能兼容。
4. P3：静态侧栏灯牌、空白页独立动效图层、五项设置。
5. P4：浅色、窄屏及打印规则。
6. P5：自动检查、真实桌面验收、安装及回退文档。

## 风险与验证

主要风险为 Obsidian 版本选择器变化、Callout 颜色格式、浅色强调色对比度、编辑器交互和用户设置优先级。优先原生变量，保持选择器简单。

自动检查覆盖 manifest、CSS 语法及可验证的属性值、设置 YAML、构建可重复性、分发一致性、资源与动效预算、SVG 路径变形结构与动态资源选用条件、两种模式基础/选中/按钮颜色对比度。对比度检查不代替完整可访问性验收。

真实桌面检查仅在实验库进行，重点观察长文阅读、实时预览和窄屏下的内容，记录应用版本与截图。中文 IME、移动端软键盘、文字间距调整、长时性能及未执行的流程必须标明未测，不能用静态检查代替。无需插件时也应正常显示。所有最终验证状态记录在 `docs/VALIDATION.md`。

## 首个正式版

1.0.0 冻结现有视觉与交互，电影场景研究暂停，不列入发布范围。当前工作为正式版打包、README 和社区目录提交准备。1.0.1 为公开提交准备版，原创软件代码使用 MIT，第三方角色设计权益按 NOTICE 单独说明，完整软件许可证进入安装 CSS。公开与提交已获用户授权，实际审核状态写入 docs/PUBLISHING.md；不在通过前宣称上架或取得单独原作授权。
